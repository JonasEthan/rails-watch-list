<h1>Add one or more Movie(s) to <%= @bookmark.list.name %></h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: @movies, input_html: { data: { controller: "tom-select" } } %>
  <%= f.submit %>
<% end %>
